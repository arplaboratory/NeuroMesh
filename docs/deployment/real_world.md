# Real-World Deployment

Use namespaced launches for each robot and ensure synchronized clocks and transforms.

Key recommendations:

- Stable network transport
- Consistent frame naming (`map`, `<robot>/odom`)
- Health checks on inference and communication topics
